# ============ Java 评分后端（已部署在服务器，只连不部署）============
# 默认用 Docker 网桥网关访问宿主机 8081；不通就改成 docker network inspect bridge 查到的网关 IP，或服务器内网 IP
JAVA_BACKEND_URL=http://172.17.0.1:8081

# ============ 大模型（阿里云百炼 OpenAI 兼容）============
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=sk-7cf9bff6604f4d6ba4d32346be083da4
LLM_MODEL=qwen3-vl-235b-a22b-instruct

# ============ 智能体鉴权（前端调用的 Bearer Key）============
# JSON 数组格式，可配置多个 Key
AUTH_API_KEYS=["app-50b15bd5240cf16958ab7f6e7d194e1f"]

# ============ 内置 MySQL（智能体会话记忆库，随 compose 一起起）============
# 已给默认值，正常情况下无需修改
MYSQL_ROOT_PASSWORD=agent-memory-2026
MEMORY_DATABASE=agent_memory
